brew install harfbuzz
brew install yaml-cpp

make versions in CMakeLists.txt match those

make configure (for cli and for compile_commands.json, needed for dev)

make configure-xcode (for .mm on mac)
make build-xcode

ableton logs
-- ~/Library/Preferences/Ableton/Live 12.1.0/Log.txt

remote scripts location
-- ~/Music/Ableton/User Library/Remote Scripts/LiveImproved
